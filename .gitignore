node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
.idea

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# --- Environmental pillar (pipeline/) ---

# Cached/regeneratable downloads -- large, and re-fetchable from URLs
# already in the scripts/README, so not worth versioning.
pipeline/pdf_extraction/pdfs/
pipeline/epa_ghgrp/*.xlsx
pipeline/epa_ghgrp/*.xlsb
pipeline/epa_ghgrp/summary_2023/
pipeline/epa_ghgrp/summary_2023.zip

# Python
__pycache__/
*.pyc

# Ephemeral chart-export artifact, not a real deliverable -- regenerate
# from data/environmental_scores.csv if needed.
pipeline/scoring/scores_for_chart.json

# Local assistant working files -- not part of the project, kept locally only.
CLAUDE.md
.claude/
